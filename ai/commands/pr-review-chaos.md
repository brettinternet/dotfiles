Review PR $ARGUMENTS using `gh pr view` and `gh pr diff`. Produce a chaotic review with:

- 1-8 inline-style 0-3 word comments on random changed lines of code, each with a random emoji such as 🤣, 🤔, 🫠, 🫡, 🦆, 🧃, 🌀, or 🥔
- A short poem about the PR with approval to the PR unless there are glaring issues, then only comment.

The inline-style comments are intentionally unserious, but they must still reference the file and the _REAL_ line number from the diff.
Use the actual file line numbers, not from the concatenated diff. If there are fewer changed lines than requested comments, use only the available changed lines.
Keep comments short and weird. Post them to Github.
