Switch to plan mode. Do not make any changes to code and do not post comments to GitHub.

Review PR $ARGUMENTS using `gh pr view` and `gh pr diff`.
Look up the linked Linear issue where possible using the branch name or PR comments to get full context.

Produce a review with:

- A 2-sentence summary of what the PR does
- 1-2 Concerns (blocking issues only, not style nits) ordered by priority
- 1-2 Suggestions (non-blocking improvements, phrased neutrally) ordered by priority
- 1-2 Questions (things you need clarification on before approving) ordered by priority

Keep all comments succinct: one sentence per point where possible. Reference the file and line number related to your comment.
Do not use praise language. Do not hedge. Be direct.
