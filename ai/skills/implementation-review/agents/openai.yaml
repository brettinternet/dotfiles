interface:
  display_name: "Implementation review"
  short_description: "Validate implementation behavior and high-impact risks"
  default_prompt: "Use $implementation-review to review the supplied branch, PR, commit range, files, or completed backlog item."
policy:
  allow_implicit_invocation: true
