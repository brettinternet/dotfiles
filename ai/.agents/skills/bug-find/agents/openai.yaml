# Generated by install-agent-commands.
interface:
  display_name: "bug find"
  short_description: "Run the bug-find command workflow"
  default_prompt: "Use $bug-find with the relevant arguments to run this command workflow."
policy:
  allow_implicit_invocation: false
