# Generated by install-agent-commands.
interface:
  display_name: "pr review loop"
  short_description: "Run the pr-review-loop command workflow"
  default_prompt: "Use $pr-review-loop with the relevant arguments to run this command workflow."
policy:
  allow_implicit_invocation: false
