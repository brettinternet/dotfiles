# Generated by install-agent-commands.
interface:
  display_name: "work status"
  short_description: "Run the work-status command workflow"
  default_prompt: "Use $work-status with the relevant arguments to run this command workflow."
policy:
  allow_implicit_invocation: false
