# Generated by install-agent-commands.
interface:
  display_name: "backlog list"
  short_description: "Run the backlog-list command workflow"
  default_prompt: "Use $backlog-list with the relevant arguments to run this command workflow."
policy:
  allow_implicit_invocation: false
