# OpenAI profile

OpenCode profile for OpenAI models and tooling.

- `ocx.jsonc` keeps the profile conservative.
- `opencode.jsonc` stays aligned with the top-level OpenCode setup.
- `oh-my-openagent.jsonc` carries the model routing.
