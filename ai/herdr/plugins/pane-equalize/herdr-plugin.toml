id = "brett.pane-equalize"
name = "Equalize tab panes"
version = "0.1.0"
min_herdr_version = "0.8.0"
description = "Give every pane in the focused tab an equal share of its layout"
platforms = ["macos"]

[[actions]]
id = "equalize"
title = "Equalize tab panes"
contexts = ["pane"]
command = ["python3", "equalize.py"]
