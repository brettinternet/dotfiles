---
- defaults:
    link:
      create: true
      relink: true

- link:
    ~/.zprofile:
      path: mac/.zprofile
    ~/.hushlogin:
      path: mac/.hushlogin
    ~/.functions/:
      glob: true
      path: mac/.functions/*

- shell:
    - ['./mac/brew.sh']
