id = "brett.pane-equalize"
name = "Equalize tab panes"
version = "0.1.2"
min_herdr_version = "0.8.0"
description = "Give panes equal width across rows and equal height within columns"
platforms = ["linux", "macos"]

[[actions]]
id = "equalize"
title = "Equalize tab panes"
contexts = ["pane"]
command = ["python3", "equalize.py"]
