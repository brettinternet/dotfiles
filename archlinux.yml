---
- defaults:
    link:
      create: true
      relink: true

- link:
    ~/.functions/pacman.sh:
      path: archlinux/.functions/pacman.sh
      # Bug: glob unsupported for single files
      # https://github.com/anishathalye/dotbot/issues/282
      # glob: true
